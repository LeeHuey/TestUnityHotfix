using UnityEngine;

public class GameCore : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello, This is GameCore Start");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
